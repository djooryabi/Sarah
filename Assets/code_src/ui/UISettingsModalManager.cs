using System.Collections;
using System.Collections.Generic;
using UnityEngine;

///
/// Manages and exposes state for the UI Settings Modal.
///
/// It should be relatively drag-n-drop into any scene's top-level Canvas
/// in order to allow it to show up.
///
public class UISettingsModalManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
